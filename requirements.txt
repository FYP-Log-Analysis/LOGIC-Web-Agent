fastapi
uvicorn[standard]
pyyaml
python-dotenv
groq
ijson
python-multipart
requests
maxminddb
# Auth
python-jose[cryptography]
bcrypt>=4.0.0
# sqlite3 is part of the Python standard library — no install needed
# Grafana runs as a separate Docker container — no Python package needed
# Next.js frontend is in frontend/ and managed by npm — no Python package needed
