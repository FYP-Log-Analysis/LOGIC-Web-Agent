# ┌──────────────────────────────────────────────────────────────────────┐
# │  LOGIC Web Agent — Docker Compose                                    │
# │                                                                      │
# │  Services:                                                           │
# │    api       → http://localhost:4000   (FastAPI + pipeline)          │
# │    dashboard → http://localhost:8501   (Streamlit)                   │
# │    grafana   → http://localhost:3000   (Grafana OSS)                 │
# │                                                                      │
# │  Grafana quick-start:                                                │
# │    1. docker compose up -d                                           │
# │    2. Open http://localhost:3000  — login: admin / logic1234         │
# │    3. Dashboard "LOGIC Web Agent — Security Overview" auto-loads     │
# │    4. Upload logs via POST /api/upload (or python run_pipeline.py)   │
# │    5. Hit ⟳ Refresh in Grafana to see new results                   │
# │                                                                      │
# │  Plugin: yesoreyeram-infinity-datasource (auto-installed at start)   │
# │  Data is pulled live from http://api:4000/api/search/ endpoints      │
# └──────────────────────────────────────────────────────────────────────┘

services:
  api:
    build:
      context: .                  # project root — gives access to all pipeline modules
      dockerfile: api/Dockerfile
    container_name: logic-webagent-api
    ports:
      - "4000:4000"
    volumes:
      - ./data:/app/data          # persist detection results + SQLite DB between runs
    env_file:
      - path: .env
        required: false           # won't fail if .env doesn't exist yet
    environment:
      - PYTHONPATH=/app
      - LM_STUDIO_BASE_URL=${LM_STUDIO_BASE_URL:-http://host.docker.internal:1234/v1}
      - LM_STUDIO_MODEL=${LM_STUDIO_MODEL:-local-model}
      - GROQ_API_KEY=${GROQ_API_KEY:-}
    restart: unless-stopped
    mem_limit: 2g
    extra_hosts:
      - "host.docker.internal:host-gateway"

  dashboard:
    build:
      context: ./dashboard
      dockerfile: Dockerfile
    container_name: logic-webagent-dashboard
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data:ro
      - ./dashboard:/app          # live-reload dashboard code without rebuilding
    depends_on:
      - api
    environment:
      - API_BASE_URL=http://api:4000
      # Cap anomaly rows loaded into dashboard memory (large files cause OOM)
      - ANOMALY_DISPLAY_LIMIT=5000
    restart: unless-stopped
    mem_limit: 768m
    extra_hosts:
      - "host.docker.internal:host-gateway"

  grafana:
    image: grafana/grafana-oss:10.4.2
    container_name: logic-grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD:-logic1234}
      - GF_INSTALL_PLUGINS=yesoreyeram-infinity-datasource
      - GF_AUTH_ANONYMOUS_ENABLED=true
      - GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer
      - GF_USERS_ALLOW_SIGN_UP=false
    depends_on:
      - api
    restart: unless-stopped
    mem_limit: 256m

volumes:
  grafana_data:
